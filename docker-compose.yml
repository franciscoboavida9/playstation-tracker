version: '3.8'
services:
  postgres:
    image: postgres:15-alpine
    container_name: ps-tracker-db
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password
      POSTGRES_DB: pstracker
    ports:
      - "5432:5432"